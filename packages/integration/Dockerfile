# TODO (task 03): Implement full integration container build
# This stub exists to establish the package structure.
FROM ubuntu:22.04
LABEL org.opencontainers.image.title="fox-in-the-box"
RUN echo "Stub — replace in task 03"
